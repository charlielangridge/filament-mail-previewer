# Changelog

All notable changes to `filament-mail-previewer` will be documented in this file.

## v0.2.1 - 2026-04-09

- Add Laravel 13 support coverage by expanding the Testbench compatibility range and CI matrix.
- Preserve the existing Filament plugin API and page registration behavior while adding tests for package boot and plugin/page registration.
- Fix package config registration so `config/mail-previewer.php` is loaded correctly, while keeping a fallback for the previous config key lookup.

## 1.0.0 - 202X-XX-XX

- initial release
